# Core app
streamlit>=1.36
numpy>=1.24
opencv-python>=4.8
PyYAML>=6.0
pillow>=10.0

# Classifiers and detectors (all three tabs)
torch>=2.1
torchvision>=0.16
timm>=1.0
ultralytics>=8.3

# CalorieCLIP (calorie tab)
open-clip-torch>=2.24

# Depth Anything V2 (portion tab)
transformers>=4.45

# SAM2 — install from source, it isn't on PyPI:
#   pip install "git+https://github.com/facebookresearch/sam2.git"
